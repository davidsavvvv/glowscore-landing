Small fully-rounded pill — score tags, "Populaire", "Gratuit" labels.

```jsx
<Badge>9.2 / 10</Badge>
<Badge variant="soft">Populaire</Badge>
<Badge variant="subtle">Gratuit</Badge>
<Badge variant="outline">Nouveau</Badge>
```

Variants: `gold` (filled, black text), `soft` (gold tint on dark), `subtle` (faint white chip), `outline` (hairline). 22px tall, 12px/700 text.

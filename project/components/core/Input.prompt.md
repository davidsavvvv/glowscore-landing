Dark text input — #1a1a1a fill, gold focus ring (border).

```jsx
<Input placeholder="Email" type="email" />
<Input placeholder="Mot de passe" type="password" />
```

Full-width by default. Placeholder text is #555. Pair with `Button block` in auth forms.

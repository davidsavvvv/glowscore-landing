Floating glass pill — the marketing landing header. Place it `position: sticky; top: 16px` inside a centered flex container.

```jsx
<GlassNav>
  <span style={{ fontFamily: "var(--gs-font-serif)", fontWeight: 700 }}>
    <span style={{ color: "var(--gs-gold)" }}>Glow</span>Score
  </span>
  <div style={{ display: "flex", gap: 24 }}>…links…</div>
  <Button variant="white" size="sm">Mon compte</Button>
</GlassNav>
```

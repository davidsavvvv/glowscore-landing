Category score row with a gold progress bar.

```jsx
<SubScore label="Structure faciale" value={8.0} />
<SubScore label="Peau" value={7.4} />
<SubScore label="Harmonie" value={7.7} />
```

Stack several inside a `Card` to build the "Scores détaillés" panel.
